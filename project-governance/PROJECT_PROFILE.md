# Current Project Profile

当前项目作为 AEGF 第一个 proving ground。

项目专属内容包括：V6 product specs、V6 architecture、core/simulation/content/web mappings、NOVA policy、investigation loop、science assurance、case-specific protected rules。

这些规则不得进入 Generic Kernel。

```yaml
assurance_modules:
  architecture: enabled
  product: enabled
  science: enabled
```

当前 Delivery Completion：merged to main + CI pass + main verified + branch cleanup。
